# Python version required to run the project
python: "3.11"

# Dependencies required to build packages (optional)
build_dependencies:
  - pip
  - setuptools
  - wheel

# Dependencies required to run the project
dependencies:
  - mlflow==3.1.0
  - scikit-learn==1.7.0
  - pandas==2.3.0
  - numpy==2.3.0
  - xgboost==3.0.2
  - matplotlib==3.10.3
  - seaborn==0.13.2