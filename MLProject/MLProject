name: stroke_prediction_mlflow_project

python_env: python_env.yaml

entry_points:
  main:
    parameters:
      experiment_name:
        type: string
        default: "stroke_prediction_ci"
        description: "Name of the MLflow experiment"
      data_path:
        type: string
        default: "stroke_data_preprocessing"
        description: "Path to the preprocessed data directory"
      verbose:
        type: bool
        default: false
        description: "Enable verbose logging"
    command: "python modelling.py --experiment-name {experiment_name} --data-path {data_path} {verbose}"
  
  test:
    parameters:
      data_path:
        type: string
        default: "stroke_data_preprocessing"
    command: "python -c 'import os; print(\"✅ MLProject structure is valid\"); print(f\"📁 Data path exists: {os.path.exists(\"{data_path}\")}\")';"